<%- if document[:id] == params[:id] -%>
  <li title="<%= document.get(Blacklight.config[:show][:heading]).to_s %>"><%= truncate(document.get(Blacklight.config[:show][:heading]).to_s, :length => 35, :omission => "...") %></li>
<%- else -%>
  <li><%= link_to_document(document, :label => truncate(document.get(Blacklight.config[:show][:heading]).to_s, :length => 35, :omission => "..."),:title=>document.get(Blacklight.config[:show][:heading]).to_s) -%></li>
<%- end -%>